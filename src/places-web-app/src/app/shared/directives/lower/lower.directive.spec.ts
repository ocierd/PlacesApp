import { LowerDirective } from './lower.directive';

describe('LowerDirective', () => {
  it('should create an instance', () => {
    const directive = new LowerDirective();
    expect(directive).toBeTruthy();
  });
});
